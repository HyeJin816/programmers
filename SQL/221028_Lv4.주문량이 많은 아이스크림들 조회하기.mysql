SELECT J.FLAVOR
FROM FIRST_HALF AS F
JOIN (SELECT FLAVOR, SUM(TOTAL_ORDER) AS TOTAL
      FROM JULY 
      GROUP BY FLAVOR) AS J
ON J.FLAVOR = F.FLAVOR
GROUP BY FLAVOR
ORDER BY SUM(F.TOTAL_ORDER) + SUM(J.TOTAL) DESC
LIMIT 3
